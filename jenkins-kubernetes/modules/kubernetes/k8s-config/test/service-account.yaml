# Kubernetes service account for the Kubernetes Go Client tests executed on the Jenkins server.
# Author: Andrew Jarombek
# Date: 7/6/2020

apiVersion: v1
kind: ServiceAccount
metadata:
  name: jenkins-kubernetes-test
  namespace: jenkins